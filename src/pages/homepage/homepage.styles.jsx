// Importing styled components from lib
import styled from 'styled-components';

// Creating styled component called HomepageContainer
// styled is from the styled-components lib
// div is the type of HTML element we wish to render
// We then place our styles in between ``
// We can use any css property
// It is like writing CSS not camel case.
export const HomePageContainer = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px 80px;

`