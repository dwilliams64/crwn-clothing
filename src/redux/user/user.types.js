// This is considered a best practice.

// To avoid errors and typos inside of our code we are going to store an object that
// will have the names of our user actions.

// We will then use this object with the property that contains the action we want
// inside of our actions and reducers.

export const UserActoinTypes = {
    SET_CURRENT_USER: 'SET_CURRENT_USER'
}